"use strict";
let age;
age = '24';
age = 25;
let cordinates = { lat: 0, lng: 1 };
cordinates = { x: 0, y: 1 };
let array1 = [1, 2, 'abb'];
//literal type 
let zero = 0;
let string1 = 'hi';
//where to use it
function answerQuestion(answer) {
    return `your selected ${answer}`;
}
